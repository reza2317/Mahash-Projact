/**
 * Google Auth / Token Utilities
 * Neutralized to prevent "Access blocked: Authorization Error" OAuth popups.
 */

export const GOOGLE_CLIENT_ID = "";

export const initGoogleDriveAuth = () => {
  // Safe no-op
};

export const getAccessToken = async (): Promise<string> => {
  return "";
};

export const hasDriveAccess = () => {
  return true;
};
